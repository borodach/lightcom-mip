CREATE TABLE LOGINS 
(   OBJECTID INT NOT NULL AUTO_INCREMENT PRIMARY KEY, 
    LOGIN VARCHAR (64) BINARY NOT NULL,
    DOMAIN VARCHAR (64) BINARY,
    PASSWORD VARCHAR (64) BINARY,
    CAPACITY INT NOT NULL,
    ENABLED BOOL NOT NULL DEFAULT 1,
    COMMENTS TEXT,

    INDEX (LOGIN)    
)